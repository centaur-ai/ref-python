name: Beta
on:
  push:
    tags-ignore:
      - v[0-9]+.[0-9]+.[0-9]+
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: 3
      - run: poetry install
      - run: python -m compileall .
      - run: flake8 .
      - run: pytest tests
